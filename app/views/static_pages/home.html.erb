<div class="row">
  <article class= "col-md-8 col-md-offset-2">
    <% if @posts.any? %>
      <ol class="posts">
        <%= render @posts %>
      </ol>
      <%= will_paginate @posts %>
    <% end %>
  </article>
</div>

